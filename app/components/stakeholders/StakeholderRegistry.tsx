"use client";

import { useMemo, useState } from "react";
import { filterStakeholders, getStakeholderMetrics, type Stakeholder, type StakeholderFilter } from "@/lib/stakeholdersData";
import { StakeholderDetail } from "@/app/components/stakeholders/StakeholderDetail";

const filters: StakeholderFilter[] = ["All", "Clients", "Contractors", "Sub-Consultants", "Regulatory Bodies"];

function Rating({ value }: { value: number }) { return <span className="inline-flex items-center gap-1 font-semibold text-amber-700"><span aria-hidden="true">★</span>{value.toFixed(1)}</span>; }

export function StakeholderRegistry() {
  const [query, setQuery] = useState("");
  const [filter, setFilter] = useState<StakeholderFilter>("All");
  const [view, setView] = useState<"table" | "cards">("table");
  const [selected, setSelected] = useState<Stakeholder | null>(null);
  const entities = useMemo(() => filterStakeholders(query, filter), [query, filter]);
  const metrics = getStakeholderMetrics();
  const cards = [{ label: "Total registered entities", value: metrics.total }, { label: "Active client accounts", value: metrics.activeClients }, { label: "Prime contractors", value: metrics.primeContractors }, { label: "Average performance score", value: `${metrics.averagePerformance.toFixed(1)} / 5` }];

  return <div className="mx-auto max-w-7xl"><section className="flex flex-col gap-5 border-b border-slate-200 pb-8 lg:flex-row lg:items-end lg:justify-between"><div className="max-w-3xl"><p className="text-xs font-bold uppercase tracking-[0.18em] text-cyan-700">Relationship management</p><h1 className="mt-3 text-3xl font-bold tracking-tight text-slate-950 sm:text-4xl">Stakeholders & clients</h1><p className="mt-3 text-base leading-7 text-slate-600">A controlled registry for clients, contractors, sub-consultants, and regulatory authorities across PMC engagements.</p></div><button type="button" className="w-fit rounded-lg bg-slate-900 px-4 py-2.5 text-sm font-semibold text-white shadow-sm hover:bg-slate-700">Register entity</button></section><section className="mt-7 grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{cards.map((card) => <article key={card.label} className="rounded-xl border border-slate-200 bg-white p-5 shadow-sm"><p className="text-sm font-medium text-slate-500">{card.label}</p><p className="mt-3 text-2xl font-bold tracking-tight text-slate-950">{card.value}</p></article>)}</section><section className="mt-7 rounded-xl border border-slate-200 bg-white p-5 shadow-sm"><div className="flex flex-col gap-4 lg:flex-row lg:items-center lg:justify-between"><label className="relative block w-full lg:max-w-md"><span className="sr-only">Search stakeholders</span><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search entity, contact, or registration ID" className="w-full rounded-lg border border-slate-300 py-2.5 pl-4 pr-4 text-sm outline-none transition focus:border-cyan-600 focus:ring-2 focus:ring-cyan-100" /></label><div className="flex rounded-lg border border-slate-200 p-1"><button type="button" onClick={() => setView("table")} className={`rounded-md px-3 py-1.5 text-sm font-semibold ${view === "table" ? "bg-slate-900 text-white" : "text-slate-600 hover:bg-slate-100"}`}>Table</button><button type="button" onClick={() => setView("cards")} className={`rounded-md px-3 py-1.5 text-sm font-semibold ${view === "cards" ? "bg-slate-900 text-white" : "text-slate-600 hover:bg-slate-100"}`}>Cards</button></div></div><div className="mt-5 flex flex-wrap gap-2">{filters.map((option) => <button key={option} type="button" onClick={() => setFilter(option)} className={`rounded-full px-3 py-1.5 text-sm font-semibold transition ${filter === option ? "bg-cyan-600 text-white" : "bg-slate-100 text-slate-600 hover:bg-slate-200"}`}>{option}</button>)}</div><p className="mt-5 text-sm text-slate-500">Showing {entities.length} of {metrics.total} registered entities</p><div className="mt-4">{view === "table" ? <div className="overflow-x-auto"><table className="w-full min-w-[940px] text-left"><thead className="border-b border-slate-200 text-xs uppercase tracking-wider text-slate-500"><tr><th className="p-3">Organization</th><th className="p-3">Tax / business ID</th><th className="p-3">Primary contact</th><th className="p-3">PMC contracts</th><th className="p-3">Performance</th></tr></thead><tbody className="divide-y divide-slate-100">{entities.map((entity) => <tr key={entity.id} onClick={() => setSelected(entity)} className="cursor-pointer transition hover:bg-slate-50"><td className="p-3"><p className="font-semibold text-slate-800">{entity.legalName}</p><p className="mt-1 text-xs text-slate-500">{entity.category} · {entity.registrationId}</p></td><td className="p-3 text-sm text-slate-600">{entity.taxId}</td><td className="p-3"><p className="text-sm font-medium text-slate-700">{entity.contacts[0]?.name}</p><p className="text-xs text-slate-500">{entity.contacts[0]?.designation}</p></td><td className="p-3 text-sm font-semibold text-slate-700">{entity.activeContracts} active</td><td className="p-3"><Rating value={entity.performanceScore} /></td></tr>)}</tbody></table></div> : <div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{entities.map((entity) => <button key={entity.id} type="button" onClick={() => setSelected(entity)} className="rounded-xl border border-slate-200 p-5 text-left shadow-sm transition hover:-translate-y-0.5 hover:border-cyan-400 hover:shadow-md"><div className="flex items-start justify-between gap-3"><span className="rounded-full bg-cyan-50 px-2.5 py-1 text-xs font-bold text-cyan-700">{entity.category}</span><Rating value={entity.performanceScore} /></div><h2 className="mt-4 text-base font-bold text-slate-900">{entity.legalName}</h2><p className="mt-1 text-sm text-slate-500">{entity.registrationId} · {entity.taxId}</p><div className="mt-5 border-t border-slate-100 pt-4"><p className="text-sm font-semibold text-slate-700">{entity.contacts[0]?.name}</p><p className="mt-1 text-xs text-slate-500">{entity.contacts[0]?.designation}</p><p className="mt-3 text-sm text-slate-600">{entity.activeContracts} active PMC contracts · {entity.activeTransmittals} transmittals</p></div></button>)}</div>}</div></section><StakeholderDetail stakeholder={selected} onClose={() => setSelected(null)} /></div>;
}
