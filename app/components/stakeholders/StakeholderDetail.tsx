"use client";

import type { Stakeholder } from "@/lib/stakeholdersData";

export function StakeholderDetail({ stakeholder, onClose }: { stakeholder: Stakeholder | null; onClose: () => void }) {
  if (!stakeholder) return null;
  return <div className="fixed inset-0 z-[60]" role="dialog" aria-modal="true" aria-labelledby="stakeholder-title"><button type="button" className="absolute inset-0 bg-slate-950/40" aria-label="Close stakeholder profile" onClick={onClose} /><aside className="absolute inset-y-0 right-0 flex w-full max-w-2xl flex-col overflow-y-auto bg-white shadow-2xl"><header className="flex items-start justify-between border-b border-slate-200 px-6 py-6"><div><p className="text-xs font-bold uppercase tracking-[0.16em] text-cyan-700">{stakeholder.category}</p><h2 id="stakeholder-title" className="mt-2 text-2xl font-bold tracking-tight text-slate-950">{stakeholder.legalName}</h2><p className="mt-1 text-sm text-slate-500">{stakeholder.registrationId} · {stakeholder.address}</p></div><button type="button" onClick={onClose} className="rounded-lg border border-slate-200 px-3 py-2 text-sm font-semibold text-slate-700 hover:bg-slate-100">Close</button></header><div className="space-y-8 p-6"><section className="grid gap-4 sm:grid-cols-3"><div className="rounded-lg bg-slate-50 p-4"><p className="text-xs font-semibold uppercase tracking-wide text-slate-500">Tax / business ID</p><p className="mt-2 text-sm font-bold text-slate-800">{stakeholder.taxId}</p></div><div className="rounded-lg bg-slate-50 p-4"><p className="text-xs font-semibold uppercase tracking-wide text-slate-500">Active contracts</p><p className="mt-2 text-lg font-bold text-slate-800">{stakeholder.activeContracts}</p></div><div className="rounded-lg bg-slate-50 p-4"><p className="text-xs font-semibold uppercase tracking-wide text-slate-500">Performance</p><p className="mt-2 text-lg font-bold text-slate-800">{stakeholder.performanceScore.toFixed(1)} / 5.0</p></div></section><section><h3 className="text-base font-bold text-slate-950">Key personnel directory</h3><div className="mt-3 overflow-hidden rounded-lg border border-slate-200"><table className="w-full text-left text-sm"><thead className="bg-slate-50 text-xs uppercase tracking-wide text-slate-500"><tr><th className="p-3">Contact</th><th className="p-3">Email</th><th className="p-3">Direct phone</th></tr></thead><tbody className="divide-y divide-slate-100">{stakeholder.contacts.map((contact) => <tr key={contact.email}><td className="p-3"><p className="font-semibold text-slate-800">{contact.name}</p><p className="text-xs text-slate-500">{contact.designation}</p></td><td className="p-3 text-slate-600">{contact.email}</td><td className="p-3 text-slate-600">{contact.phone}</td></tr>)}</tbody></table></div></section><section><h3 className="text-base font-bold text-slate-950">Linked PMC projects & contract history</h3><div className="mt-3 space-y-2">{stakeholder.projects.map((project) => <article key={project.contractRef} className="flex flex-col justify-between gap-2 rounded-lg border border-slate-200 p-4 sm:flex-row sm:items-center"><div><p className="font-semibold text-slate-800">{project.project}</p><p className="mt-1 text-sm text-slate-500">{project.role} · {project.contractRef}</p></div><span className={`w-fit rounded-full px-2.5 py-1 text-xs font-bold ${project.status === "Active" ? "bg-emerald-100 text-emerald-700" : "bg-slate-100 text-slate-600"}`}>{project.status}</span></article>)}</div></section><section><div className="flex items-end justify-between"><h3 className="text-base font-bold text-slate-950">Transmittals & communication logs</h3><span className="text-sm font-semibold text-cyan-700">{stakeholder.activeTransmittals} active transmittals</span></div><ol className="mt-3 space-y-3 border-l-2 border-slate-200 pl-4">{stakeholder.communicationLogs.map((log) => <li key={`${log.date}-${log.subject}`}><p className="text-sm font-semibold text-slate-800">{log.subject}</p><p className="mt-1 text-xs text-slate-500">{log.date} · {log.channel}</p></li>)}</ol></section></div></aside></div>;
}
