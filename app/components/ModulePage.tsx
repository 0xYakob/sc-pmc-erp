import Link from "next/link";

interface ModulePageProps { eyebrow: string; title: string; description: string; primaryAction: string; cards: { label: string; value: string; detail: string }[]; workflow: string[]; }

export function ModulePage({ eyebrow, title, description, primaryAction, cards, workflow }: ModulePageProps) {
  return <div className="mx-auto max-w-7xl"><section className="flex flex-col gap-6 border-b border-slate-200 pb-8 md:flex-row md:items-end md:justify-between"><div className="max-w-3xl"><p className="text-xs font-bold uppercase tracking-[0.18em] text-cyan-700">{eyebrow}</p><h1 className="mt-3 text-3xl font-bold tracking-tight sm:text-4xl">{title}</h1><p className="mt-3 text-base leading-7 text-slate-600">{description}</p></div><div className="flex gap-3"><button type="button" className="rounded-lg border border-slate-300 bg-white px-4 py-2.5 text-sm font-semibold text-slate-700 shadow-sm hover:bg-slate-100">View reports</button><Link href="#workspace" className="rounded-lg bg-slate-900 px-4 py-2.5 text-sm font-semibold text-white shadow-sm hover:bg-slate-700">{primaryAction}</Link></div></section><section id="workspace" className="mt-8 grid gap-5 lg:grid-cols-3">{cards.map((card) => <article key={card.label} className="rounded-xl border border-slate-200 bg-white p-5 shadow-sm"><p className="text-sm font-medium text-slate-500">{card.label}</p><p className="mt-3 text-2xl font-bold tracking-tight">{card.value}</p><p className="mt-2 text-sm text-slate-500">{card.detail}</p></article>)}<article className="rounded-xl border border-slate-200 bg-white p-6 shadow-sm lg:col-span-2"><h2 className="text-base font-bold">Operational workflow</h2><ul className="mt-4 divide-y divide-slate-100">{workflow.map((item) => <li key={item} className="py-3 text-sm text-slate-600">{item}</li>)}</ul></article></section></div>;
}
