"use client";

import { formatTenderCurrency, type Tender } from "@/lib/tendersData";

export function TenderDetail({ tender, onClose }: { tender: Tender | null; onClose: () => void }) {
  if (!tender) return null;
  return <div className="fixed inset-0 z-[60]" role="dialog" aria-modal="true" aria-labelledby="tender-title"><button type="button" aria-label="Close proposal details" onClick={onClose} className="absolute inset-0 bg-slate-950/40" /><aside className="absolute inset-y-0 right-0 w-full max-w-3xl overflow-y-auto bg-white shadow-2xl"><header className="flex items-start justify-between border-b border-slate-200 px-6 py-6"><div><p className="text-xs font-bold uppercase tracking-[0.16em] text-cyan-700">{tender.stage}</p><h2 id="tender-title" className="mt-2 text-2xl font-bold tracking-tight text-slate-950">{tender.title}</h2><p className="mt-1 text-sm text-slate-500">{tender.reference} · {tender.client} · {tender.location}</p></div><button type="button" onClick={onClose} className="rounded-lg border border-slate-200 px-3 py-2 text-sm font-semibold text-slate-700 hover:bg-slate-100">Close</button></header><div className="space-y-8 p-6"><section><h3 className="text-base font-bold">Key dates</h3><div className="mt-3 grid gap-3 sm:grid-cols-2">{Object.entries({ "Pre-bid conference": tender.dates.preBid, "Site visit": tender.dates.siteVisit, "Technical opening": tender.dates.technicalOpening, "Financial opening": tender.dates.financialOpening }).map(([label, value]) => <div key={label} className="rounded-lg bg-slate-50 p-4"><p className="text-xs font-semibold uppercase tracking-wide text-slate-500">{label}</p><p className="mt-2 text-sm font-bold text-slate-800">{value}</p></div>)}</div></section><section><h3 className="text-base font-bold">Personnel assignment matrix</h3><div className="mt-3 overflow-x-auto rounded-lg border border-slate-200"><table className="w-full min-w-[620px] text-left text-sm"><thead className="bg-slate-50 text-xs uppercase tracking-wide text-slate-500"><tr><th className="p-3">Proposed person</th><th className="p-3">Assignment</th><th className="p-3">Expertise</th><th className="p-3 text-right">Man-months</th></tr></thead><tbody className="divide-y divide-slate-100">{tender.personnel.map((person) => <tr key={person.name}><td className="p-3 font-semibold text-slate-800">{person.name}</td><td className="p-3 text-slate-600">{person.role}</td><td className="p-3 text-slate-600">{person.expertise}</td><td className="p-3 text-right font-semibold">{person.manMonths}</td></tr>)}</tbody></table></div></section><section><h3 className="text-base font-bold">Financial proposal scoring & competitor pricing</h3><div className="mt-3 grid gap-4 md:grid-cols-3"><div className="rounded-lg bg-slate-50 p-4"><p className="text-xs font-semibold uppercase tracking-wide text-slate-500">Technical weight</p><p className="mt-2 text-xl font-bold">{tender.financial.technicalWeight}%</p></div><div className="rounded-lg bg-slate-50 p-4"><p className="text-xs font-semibold uppercase tracking-wide text-slate-500">Financial weight</p><p className="mt-2 text-xl font-bold">{tender.financial.financialWeight}%</p></div><div className="rounded-lg bg-cyan-50 p-4"><p className="text-xs font-semibold uppercase tracking-wide text-cyan-700">Our proposed price</p><p className="mt-2 text-xl font-bold text-cyan-900">{tender.financial.ourPriceUsd ? formatTenderCurrency(tender.financial.ourPriceUsd, "USD") : "EOI stage"}</p></div></div>{tender.financial.competitors.length > 0 && <div className="mt-3 space-y-2">{tender.financial.competitors.map((competitor) => <div key={competitor.bidder} className="flex justify-between rounded-lg border border-slate-200 px-4 py-3 text-sm"><span className="font-medium text-slate-700">{competitor.bidder}</span><span className="font-bold text-slate-900">{formatTenderCurrency(competitor.priceUsd, "USD")}</span></div>)}</div>}</section><section><h3 className="text-base font-bold">Submission checklist</h3><div className="mt-3 grid gap-2 sm:grid-cols-2">{tender.checklist.map((item) => <div key={item.item} className={`rounded-lg border p-3 text-sm font-semibold ${item.complete ? "border-emerald-200 bg-emerald-50 text-emerald-800" : "border-amber-200 bg-amber-50 text-amber-800"}`}>{item.complete ? "Completed" : "Open"} · {item.item}</div>)}</div></section></div></aside></div>;
}
