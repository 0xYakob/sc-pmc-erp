"use client";

import { approvedVariationValue, formatContractCurrency, milestoneValue, revisedContractValue, type Contract } from "@/lib/contractsData";

const billingColors = { Unbilled: "bg-amber-100 text-amber-800", Invoiced: "bg-sky-100 text-sky-700", Paid: "bg-emerald-100 text-emerald-700" };

export function ContractDetail({ contract, onClose, onLogVariation }: { contract: Contract | null; onClose: () => void; onLogVariation: () => void }) {
  if (!contract) return null;
  const financials = [{ label: "Original contract price", value: formatContractCurrency(contract.originalPriceUsd) }, { label: "Approved variation orders", value: formatContractCurrency(approvedVariationValue(contract)) }, { label: "Revised contract value", value: formatContractCurrency(revisedContractValue(contract)) }, { label: "Cumulative disbursements", value: formatContractCurrency(contract.disbursedUsd) }];
  return <div className="fixed inset-0 z-[60]" role="dialog" aria-modal="true" aria-labelledby="contract-title"><button type="button" aria-label="Close contract details" className="absolute inset-0 bg-slate-950/40" onClick={onClose} /><aside className="absolute inset-y-0 right-0 w-full max-w-3xl overflow-y-auto bg-white shadow-2xl"><header className="flex items-start justify-between border-b border-slate-200 px-6 py-6"><div><p className="text-xs font-bold uppercase tracking-[0.16em] text-cyan-700">{contract.type}</p><h2 id="contract-title" className="mt-2 text-2xl font-bold tracking-tight">{contract.title}</h2><p className="mt-1 text-sm text-slate-500">{contract.reference} · {contract.project}</p></div><div className="flex gap-2"><button type="button" onClick={onLogVariation} className="rounded-lg bg-slate-900 px-3 py-2 text-sm font-semibold text-white">Log VO</button><button type="button" onClick={onClose} className="rounded-lg border border-slate-200 px-3 py-2 text-sm font-semibold">Close</button></div></header><div className="space-y-8 p-6"><section><h3 className="text-base font-bold">Financial summary</h3><div className="mt-3 grid gap-3 sm:grid-cols-2">{financials.map((item) => <div key={item.label} className="rounded-lg bg-slate-50 p-4"><p className="text-xs font-semibold uppercase tracking-wide text-slate-500">{item.label}</p><p className="mt-2 text-lg font-bold text-slate-900">{item.value}</p></div>)}</div></section><section><h3 className="text-base font-bold">Key dates & time extensions</h3><div className="mt-3 grid gap-3 sm:grid-cols-2 xl:grid-cols-4">{[{ label: "Original completion", value: contract.originalCompletion }, { label: "Approved extension", value: `${contract.extensionDays} days` }, { label: "Revised completion", value: contract.revisedCompletion }, { label: "LD exposure", value: formatContractCurrency(contract.ldExposureUsd) }].map((item) => <div key={item.label} className="rounded-lg border border-slate-200 p-4"><p className="text-xs font-semibold uppercase tracking-wide text-slate-500">{item.label}</p><p className="mt-2 text-sm font-bold text-slate-800">{item.value}</p></div>)}</div></section><section><h3 className="text-base font-bold">Payment milestone schedule</h3><div className="mt-3 overflow-x-auto rounded-lg border border-slate-200"><table className="w-full min-w-[760px] text-left text-sm"><thead className="bg-slate-50 text-xs uppercase tracking-wide text-slate-500"><tr><th className="p-3">Deliverable</th><th className="p-3">Fee</th><th className="p-3">Value</th><th className="p-3">Due date</th><th className="p-3">Billing status</th></tr></thead><tbody className="divide-y divide-slate-100">{contract.milestones.map((milestone) => <tr key={milestone.id}><td className="p-3 font-medium text-slate-800">{milestone.description}</td><td className="p-3 text-slate-600">{milestone.feePercentage}%</td><td className="p-3 font-semibold">{formatContractCurrency(milestoneValue(contract, milestone))}</td><td className="p-3 text-slate-600">{milestone.dueDate}</td><td className="p-3"><span className={`rounded-full px-2 py-1 text-xs font-bold ${billingColors[milestone.status]}`}>{milestone.status}</span></td></tr>)}</tbody></table></div></section><section><h3 className="text-base font-bold">Variation register</h3><div className="mt-3 space-y-2">{contract.variations.length > 0 ? contract.variations.map((variation) => <div key={variation.id} className="flex flex-col justify-between gap-2 rounded-lg border border-slate-200 p-4 sm:flex-row"><div><p className="font-semibold text-slate-800">{variation.title}</p><p className="mt-1 text-sm text-slate-500">{variation.reason} · {variation.timeImpactDays >= 0 ? "+" : ""}{variation.timeImpactDays} days</p></div><div className="text-right"><p className="font-bold text-slate-900">{formatContractCurrency(variation.costImpactUsd)}</p><p className="mt-1 text-xs font-semibold text-cyan-700">{variation.status}</p></div></div>) : <p className="rounded-lg bg-slate-50 p-4 text-sm text-slate-600">No variation orders have been recorded for this contract.</p>}</div></section></div></aside></div>;
}
